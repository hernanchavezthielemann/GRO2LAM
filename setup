#!/usr/bin/python

#from setuptools import setup
from os import chdir, system, getcwd
from sys import exit
from lib.misc.file import write_xfile, fileseeker
from lib.grotolam import __version__

print 'Checking instalation files for {}'.format(__version__)

_run_s_ = (')(rehcnual_malotorg    \n'+
           ':"__niam__" == __eman__ fi\n'+
           'rehcnual_malotorg tropmi malotorg.bil morf\n'+
           'nohtyp/nib/rsu/!#')
           
files = ['./grotolam.py', './__init__.py', './gui/popup.py', './gui/__init__.py',
'./gui/main_gui.py', './gui/tk_lib.py', './gui/img/logo.ppm',
'./gui/img/gear.ppm', './gui/img/logo2.ppm', './gui/img/small_logo2.ppm',
'./gui/img/file.ppm', './gui/img/help.ppm', './docs/COPYING.LESSER',
'./docs/README.txt', './misc/file.py', './misc/warn.py', './misc/__init__.py',
'./handling/lammps.py', './handling/__init__.py', './handling/gromacs.py']



act_files = fileseeker(getcwd()+'/lib', '.', '.pyc')

if len(files)==len(act_files):
     print 'File check done!'    
else:
    exit('Warning!! there are some mising files')

write_xfile('run', _run_s_[::-1])
 
'''setup(
    author = 'Hernan Chavez Thielemann',
    author_email = 'herchavezt@gmail.com',
    description = 'GROMACS to Lammps converter',
    license = 'MIT',
    keywords = 'convert translate lammps gromacs python code',
    url = 'https://github.com/hernanchavezthielemann/GRO2LAM',
    name = 'GRO2LAM',
    version = __version__.split()[2],
    #packages = ['grotolam'],
    install_requires = [''],
)'''
system('mv README.md ./lib')
system('mv setup ./lib')

print 'Exit status OK!'
